extends ./user.pug

block user-body
	+control("notes")
	div.item-list
		| {{range $i, $e := .pageUser.Notes}}
		div.list-item
			div.item-left
				span.item-number {{$i}}
			div.item-right
				div.item-title
					a(href="/note/{{$e.ID}}") {{$e.Title}}
				div.item-description {{$e.Description}}
		| {{end}}
